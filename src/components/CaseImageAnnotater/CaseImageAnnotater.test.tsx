import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import { CaseImageAnnotater } from './CaseImageAnnotater';

describe('<CaseImageAnnotater />', () => {
  test('it should mount', () => {
    // render(<CaseImageAnnotater />);
    
    // const caseImageAnnotater = screen.getByTestId('CaseImageAnnotater');

    // expect(caseImageAnnotater).toBeInTheDocument();
  });
});